Plan before implementing. This is mandatory.
Output: PR class, Branch name, File list (max 5, exact paths, create/modify/delete),
Risks/blast radius, Verification commands (type-check, lint, test, build, gate:platinum),
Rollback note. Wait for approval before implementing.
