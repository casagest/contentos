Show development status. Run: git branch --show-current, git status --short, git log --oneline -5,
git stash list, gh pr list --state open --limit 10, ls .ralph/ 2>/dev/null.
Format as dashboard with Branch, Status, Commits, Stashes, PRs, Ralph Loops, Context%, Model, Cost.
